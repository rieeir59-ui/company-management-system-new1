rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Any authenticated user can read, write, and delete files.
    match /{allPaths=**} {
      allow read, write, delete: if request.auth != null;
    }
  }
}
